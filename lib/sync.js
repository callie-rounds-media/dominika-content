// retired. the calendar is read-only on the public site; edits are done
// locally by callie and pushed to the repo. kept as a stub so any cached
// imports don't 404 mid-deploy.
export const REPO = 'callie-rounds-media/dominika-content';
